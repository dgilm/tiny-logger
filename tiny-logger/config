
crypt:
    # Available algorithms are: md5(), sha1(), sha224(), sha256(), sha384()
    crypto_algorithm: sha1
    # String to combine with actual data
    secret_shared_key: "secret-shared-key"

index:
    # Database path for index/search engine
    database_path: "/var/lib/tiny-logger/index.db"

storage:
    # Where the logs will be stored
    log_directory: "/var/lib/tiny-logger/logs"

